import { PREFIX } from "./general"

export const PARAMETERS_ATTRIBUTE = PREFIX + "code-parameters"
export const EMPTY_PARAMETERS_ATTRIBUTE = PREFIX + "code-empty-parameters"
export const CONTENT_ATTRIBUTE = PREFIX + "code-content"
export const DETECTING_CONTEXT = PREFIX + "detected-context"
